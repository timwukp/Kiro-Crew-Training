# Python virtual environment (do not commit)
venv/
__pycache__/
*.pyc
pyvenv.cfg

# Backup files
*.old

# Office temp/lock files
~$*

# OS files
.DS_Store

# PPTX generator script — the built kiro-crew-training.pptx ships in the repo,
# so end users don't need to regenerate it. Kept locally, excluded from the repo.
generate_pptx.py
